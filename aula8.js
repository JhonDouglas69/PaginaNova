//const numero; /*const não pode ser criada sem valor definido*/ 
const numero = 1;
console.log(numero);
//SyntaxError Na maior parte dos casos ocorre quando há erros no programa e o JavaScript não consegue executá-los


console.log(minhaVAR);
/*ReferenceError Normalmente ocorre quando o codigo tenta acessar algo que não existe 
outros tipo de erro
RangeError: quando o codigo recebe um dado do tipo certo, porém não está no formato aceitavel
Por exemplo: um processamento que só aceita valores inteiros positivos , mas recebe -1;
typeError indica que o codigo esperava receber um tipo, tal qual um String de texto
mas 

*/


